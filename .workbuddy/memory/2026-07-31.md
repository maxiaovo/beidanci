# 2026-07-31 工作记录

## 千问 TTS 改造（分支 feat/qwen-tts-approval）

按 docs/qwen-tts-plan.md + docs/handoff-qwen-tts.md 在 vocab-app 实施千问 TTS 接入 + 导入音频审批流 + 重新生成增强。

### 关键实测发现（与原计划重大偏差）
- 千问 TTS **不是 OpenAI 兼容 /audio/speech**：是 DashScope 原生接口
  `POST /api/v1/services/aigc/multimodal-generation/generation`，返回 JSON 里的 `output.audio.url`，需再下载 WAV。
- 模型名 `qwen-audio-3.0-tts-flash` 不存在；真实 `qwen3-tts-flash`（Jennifer/Ryan/Katerina/Aiden 4 音色均可用）。
- instruction 仅 `qwen3-tts-instruct-flash` 支持，但该模型不支持这 4 音色。折中：默认 `qwen3-tts-flash` + 随机 4 音色，instruction 尽力透传（被忽略不报错）。

### 改动文件
- lib/settings.ts：默认 Base URL/模型/语音池 EN_TTS_VOICES/DEFAULT_TTS_INSTRUCTION，TTSConfig 加 instruction
- lib/openai-tts.ts：改写 synthesizeSpeech 走 DashScope（POST→取URL→下载WAV，带重试）
- lib/tts.ts：随机音色回传 + 默认 instruction + altText 替代拼写
- lib/import-runner.ts：分析后查 audioApproved→pending_audio 暂停；approveBookAudio；resumeImport 置 audioApproved
- prisma/schema.prisma + 迁移 20260731081523_add_book_audio_approved：Book.audioApproved + pending_audio
- 新路由 app/api/admin/audio/approve/route.ts
- app/api/admin/{config,tts-preview,tts-status,audio} 路由：保存/透传 instruction、altText；tts-status 改探 DashScope 兼容 /models
- app/admin/page.tsx：音色试听列表 + instruction 输入 + 重新生成面板（临时指令+替代拼写）+ 待批准区块

### 验证结果
- tsc 通过；改动文件 lint 干净（仓库其他文件有预先存在的 react-hooks 新规则报错，与本次无关）
- tsx 实测：4 音色合成 + 带指令合成 ALL_OK；synthesize 包装层（随机音色/altText/指令/写文件）WRAP_OK
- 审批流真实演练：导入→pending_audio→批准→ready，6 个音频文件全部生成 APPROVE_FLOW_OK

### 提交（已完成）
- 用户确认「全部提交」。commit `49d3e7c` 落在 `feat/qwen-tts-approval`（16 文件，+650/−114）。
- 提交范围：lib/(4) + app/admin/page.tsx + 4 个 admin 路由（含新增 approve）+ prisma/(schema+迁移) + docs/(计划+交接，此前从未入库) + .codebase-memory/(已重建索引)。
- 未纳入：.workbuddy/（记忆目录，不提交）；.env（gitignore）。
- 已知小幅漂移：提交后 .codebase-memory/ 又被后台重建（活产物，仅时间戳/哈希差异），与 commit 69090b7 既有模式一致，功能不受影响。
- 分支仅本地提交，未 push（外部动作，需用户另行指示）。

### 待办（可选收尾，未做）
- 更新 AGENTS.md / CLAUDE.md 里过时的 TTS 描述（计划步骤 6，可选）。
- 仓库预存 lint 错误（check/page、TypingTrainer、words/page、layout 等，react-hooks/set-state-in-effect、purity 新规则）未处理，超出本次范围。

### 关键澄清：千问AI平台文档 = DashScope 同一服务（2026-07-31 用户提问核实）
- 用户给的官方文档 platform.qianwenai.com/docs/api-reference/speech-synthesis/qwen-tts 与 voice-list 已核对：
  - 千问AI平台底层就是 DashScope：官方 Python 示例明写 `dashscope.base_http_api_url='https://dashscope.aliyuncs.com/api/v1'`。所以平台域名 ≠ 不同服务，端点与 dashscope.aliyuncs.com 完全一致。
  - 端点 `POST /api/v1/services/aigc/multimodal-generation/generation`、鉴权 `Authorization: Bearer`、响应 `output.audio.url` 二次下载 —— 与已实现一致。
  - 4 音色 Jennifer/Ryan/Katerina/Aiden 在 voice-list 确认存在，且非实时均支持 `qwen3-tts-flash`（与实现一致）。
  - instruction 仅 Qwen3-TTS-Instruct-Flash 系列生效，且该系列不支持这 4 音色（与实现折中一致）。
- 偏差修正：instructions 官方 HTTP 规范要求嵌套在 `input` 下（非顶层）。已修 lib/openai-tts.ts 并 commit 0021547。
- 结论：实现符合用户给的官方文档；原计划的 OpenAI 兼容 /audio/speech 假设确为错误，DashScope 原生端点才是对的。

### 凭据
- TTS_API_TOKEN 已写入 .env（gitignore 已忽略），勿提交。
